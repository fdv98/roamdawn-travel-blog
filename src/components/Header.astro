---
<header class="site-header">
  <div class="header-inner">
    <a href="/" class="logo" aria-label="RoamDawn home"><span>Roam</span><b>Dawn</b></a>

    <nav class="desktop-nav" aria-label="Primary navigation">
      <a href="/blog/">Stories</a>
      <a href="/destinations/">Destinations</a>
      <a href="/itineraries/">Travel Guides</a>
      <a href="/tags/travel-planning/">Travel Tips</a>
      <a href="/about/">About</a>
      <a class="search-link" href="/search/">Search</a>
    </nav>

    <button class="menu-button" type="button" aria-expanded="false" aria-controls="mobile-nav">Menu</button>
  </div>

  <div id="mobile-nav" class="mobile-nav" hidden>
    <nav aria-label="Mobile navigation">
      <a href="/blog/">Stories</a>
      <a href="/destinations/">Destinations</a>
      <a href="/itineraries/">Travel Guides</a>
      <a href="/tags/travel-planning/">Travel Tips</a>
      <a href="/about/">About</a>
      <a href="/search/">Search</a>
    </nav>
  </div>
</header>

<script>
  const menuButton = document.querySelector('.menu-button');
  const mobileNav = document.querySelector('#mobile-nav');

  if (menuButton && mobileNav) {
    menuButton.addEventListener('click', () => {
      const open = menuButton.getAttribute('aria-expanded') === 'true';
      menuButton.setAttribute('aria-expanded', String(!open));
      mobileNav.hidden = open;
    });

    mobileNav.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', () => {
        menuButton.setAttribute('aria-expanded', 'false');
        mobileNav.hidden = true;
      });
    });
  }
</script>

<style>
  .site-header { background: #fff; border-bottom: 1px solid #e5e7e8; }
  .header-inner { width: min(1120px, calc(100% - 32px)); min-height: 72px; margin: 0 auto; display: flex; align-items: center; justify-content: space-between; gap: 24px; }
  .logo { color: #18262c; text-decoration: none; font-size: 1.65rem; font-weight: 800; letter-spacing: -.06em; }
  .logo span { color: #18262c; }
  .logo b { color: #197a91; font-weight: 800; }
  .desktop-nav { display: flex; align-items: center; gap: 27px; }
  .desktop-nav a { color: #36434a; font-size: .82rem; font-weight: 600; text-decoration: none; }
  .desktop-nav a:hover { color: #197a91; }
  .desktop-nav .search-link { padding-left: 8px; color: #197a91; }
  .menu-button { display: none; border: 1px solid #d9dfe1; background: #fff; color: #26343a; border-radius: 5px; padding: 8px 11px; font-size: .8rem; font-weight: 700; }
  .mobile-nav { border-top: 1px solid #e5e7e8; background: #fff; }
  .mobile-nav nav { width: min(1120px, calc(100% - 32px)); margin: 0 auto; display: grid; }
  .mobile-nav a { padding: 14px 0; border-bottom: 1px solid #eef0f1; color: #26343a; text-decoration: none; font-size: .9rem; font-weight: 600; }
  .mobile-nav a:last-child { border-bottom: 0; }
  @media (max-width: 760px) {
    .header-inner { min-height: 64px; width: calc(100% - 24px); }
    .desktop-nav { display: none; }
    .menu-button { display: block; }
  }
</style>
